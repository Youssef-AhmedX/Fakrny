@inherits BasePage<SectionPostDto>

    <MudDialog>
    <DialogContent>
        @if (sectionForm is null)
        {
            <MudCard Outlined="true" Style="border:0px;text-align:center">
                <MudCardContent>
                    <MudProgressCircular Color="Color.Info" Style="height:70px;width:70px;" Indeterminate="true" />
                </MudCardContent>
                <MudCardActions>
                </MudCardActions>
            </MudCard>
        }
        else
        {
            <MudCard Style="border:0px;" Outlined="true">
                <EditForm Model="@sectionForm" OnValidSubmit="OnValidSubmit">
                    <DataAnnotationsValidator />
                    <MudCardContent Style="padding-top: 0px">
                        <MudTextField Class="mb-5" HelperText="Required" Variant="Variant.Outlined" @bind-Value="sectionForm.Name" For="@(() => sectionForm.Name)" />
                        <MudTextField Class="mb-5" HelperText="Required" Variant="Variant.Outlined" @bind-Value="sectionForm.Number" For="@(() => sectionForm.Number)" />
                        <MudNumericField Class="mb-5" HideSpinButtons="true" HelperText="Not Required" @bind-Value="sectionForm.OrderIndex" For="@(() => sectionForm.OrderIndex)" Variant="Variant.Outlined" />
                        <MudTextField Class="mb-5" HelperText="Required" Variant="Variant.Outlined" Lines="5" @bind-Value="sectionForm.Description" For="@(() => sectionForm.Description)" />
                    </MudCardContent>
                    <MudCardActions>
                        <MudCheckBox Dense="true" @bind-Checked="@sectionForm.IsDeleted" For="@(() => sectionForm.IsDeleted)"
                                 Label="Mark As Deleted" Color="Color.Error"></MudCheckBox>
                        <MudSpacer />
                        <MudButton Class="me-1" Variant="Variant.Outlined" OnClick="Cancel">Cancel</MudButton>
                        <MudButton Variant="isDisable ? Variant.Text : Variant.Outlined" ButtonType="ButtonType.Submit" Disabled="isDisable" Color="Color.Success">
                            @if (isDisable)
                            {
                                <MudProgressCircular Color="Color.Success" Size="Size.Small" Indeterminate="true" />
                            }
                            else
                            {
                                <MudText>Submit</MudText>
                            }
                        </MudButton>
                    </MudCardActions>
                </EditForm>
            </MudCard>
        }
    </DialogContent>
    <DialogActions>
    </DialogActions>
</MudDialog>